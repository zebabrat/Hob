/** Minimal by design — a name and a line, not a sitemap. */
export function Footer() {
  return (
    <footer className="mt-auto border-t border-border-weak py-8 text-center">
      <p className="font-mono text-xs tracking-[0.1em] text-text-secondary uppercase">Hob</p>
      <p className="mt-1 text-xs text-text-secondary">
        A quiet place to keep track of job applications.
      </p>
    </footer>
  )
}
