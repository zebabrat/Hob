/** recharts' Tooltip contentStyle, shared so every chart's tooltip looks the same. */
export const TOOLTIP_STYLE = {
  background: 'var(--viz-surface)',
  border: '1px solid var(--viz-grid)',
  borderRadius: 8,
  fontSize: 12,
}
